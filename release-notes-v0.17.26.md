## v0.17.26 (2026-07-27)

### マクロ外部コマンドの上級者モード
- `process.runAdvanced(...)`を追加し、現在のWindows利用者権限でCLIを実行して終了コード、標準出力、timeoutを扱えるようにしました。
- `process.openAdvanced(...)`を追加し、Windowsの関連付けを使ったURL、ショートカット、文書ファイル等の起動を選択できるようにしました。このAPIは起動受付だけを返し、関連付け先の終了や出力は追跡しません。
- 上級者モードは設定画面で明示的に有効化し、各マクロ定義を初回実行時に確認します。定義変更、モード変更、全体OFFでは保存済み承認を再利用しません。

### セキュリティと信頼性
- 従来の安全モード`process.run(...)`は、OS隔離が完成するまで引き続き実行を拒否します。上級者モードへ自動的に切り替わることはありません。
- global consentとマクロ定義承認をDPAPIで保護し、設定の手編集、保護データ破損、承認hash・mode・epoch不一致をfail closedで拒否します。
- 1要求ごとの短命broker、request hash、nonce、有効期限、出力上限、timeout、kill-on-close Jobを追加しました。
- 無効化、個別取消、全取消、緊急停止と同時に実行要求が発生しても、失効前の承認ですり抜けないようにしました。
- 上級者モードはsandboxではなく現在利用者と同じ権限で動くことを、設定画面と承認画面に明示します。
