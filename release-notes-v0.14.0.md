# ClipXform v0.14.0

## 新機能

### スマート・スニペット (Smart Snippets)
定型文の中で動的な値を扱えるようになりました。以下のプレースホルダーに対応しています。

- `{{DATE}}` / `{{日付}}`: 現在の日付を挿入します。
- `{{TIME}}` / `{{時刻}}`: 現在の時刻を挿入します。
- `{{CLIPBOARD}}` / `{{クリップボード}}`: クリップボードの内容を挿入します。
- `{{INPUT:ラベル}}` / `{{入力:ラベル}}`: スニペット使用時に入力ダイアログを表示し、入力されたテキストを挿入します。

これにより、日報の作成や定型メールの送信など、より柔軟なテキスト生成が可能になります。

## 変更点

- 定型文のコピー処理が非同期化され、ユーザー入力を受け付けることができるようになりました。
- 従来の `<date>...</date>` マクロも引き続きサポートされます。

## 開発者向け

- `SmartSnippetProcessor` クラスが追加され、スニペット処理ロジックが一新されました。
- `IDialogService` を使用したモーダルダイアログの実装例が追加されました。
