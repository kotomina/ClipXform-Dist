## v0.17.24 (2026-06-07)

### 新機能 (Features)
- **マクロの保存先選択ダイアログ**: JavaScript マクロで `ui.saveFile(...)` と `ui.selectFolder(...)` を使い、実行時に Windows の保存先ファイル / フォルダ選択ダイアログから出力先を指定できるようにしました。
- **特権マクロのコマンド許可追加**: 未登録の `process.run(...)` 実行確認から、対象コマンドを許可済みコマンドへ追加して次回以降の確認を省略できるようにしました。

### 改善 (Improved)
- **OS コモン UI の整理**: マクロから扱う OS コモン UI の棚卸しを行い、保存先指定を host API として提供し、任意ファイル読み取りや印刷などは安全設計が必要なため保留 / 対象外として整理しました。
